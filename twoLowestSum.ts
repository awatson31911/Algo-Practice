function twoLowestSum (numArr: number[] ): number  {
    const numHash = {};
    let lowestNum = numArr[0], secondLowest = numArr[1];
    
    for (const num of numArr) {
        
    }
    return lowestNum + secondLowest;
 
 }